# 个人展示网站 (Personal Portfolio) — 实施计划

## 核心创意：AI 构建过程可视化 (The MVP Concept)
用户的创意非常棒：既然考核重点是 **“拥抱 AI 辅助”** 和 **“MVP 快速迭代”**，我们可以在网站中内置一个 **"AI 开发者模式 (AI Dev Mode)"** 切换开关。
- **普通模式**：展示精美的个人简历内容。
- **AI 模式开启**：网页上会浮现出对应的**生成 Prompt**、**迭代记录**或一个**悬浮的代码终端 (Terminal)**。
这样不仅展现了产品，更直接展示了**“你是如何用 AI 造出这个产品的”**，完美契合评估要求！

---

## 一、方案设计（设计风格 & 核心功能）

### 1. 设计风格 (Design System)
- **主题思想**：极客风 (Geek) + 现代简约 (Modern Minimal)
- **配色方案**：
  - 主色调：暗夜黑 (`#0A0A0A`) 作为背景，体现极客与科技感。
  - 强调色：终端绿 (`#00FF41`) 或 AI 蓝/紫渐变用于高亮关键字和 Prompt。
  - 文字色：灰白 (`#EDEDED`) 为主。
- **排版 (Typography)**：
  - 中文：系统默认黑体（思源黑体/苹方）。
  - 英文 & Prompt 展示区域：使用等宽字体 (Monospace，如 `Fira Code` 或 `JetBrains Mono`)，增强代码感。
- **特效**：
  - 毛玻璃效果 (Glassmorphism) 用于卡片和导航栏。
  - 打字机动效 (Typewriter effect) 用于展示 Prompt 的生成过程。

### 2. "AI Dev Mode" 交互方案
我们准备在页面右上角放置一个醒目的 **Toggle 开关**（类似暗黑模式切换）：
- **默认状态**：直接展示美观的各模块（Hero 介绍、经历、项目等）。
- **开启 AI Dev Mode**：
  - 页面左侧或右侧滑出一个 **"Prompt 视窗 (Prompt Window)"**。
  - 当用户滚动到特定区域（如“项目展示”）时，Prompt 视窗会**同步打字出现**当初生成这个区域的 Prompt 指令。
  - 或者在每个版块旁边加上一个小小的 `[查看生成咒语]` 按钮，点击翻转卡片显示 Prompt。

---

## 二、技术选型 (Tech Stack)

| 领域 | 技术栈 | 选择理由 |
|------|-------|---------|
| **框架** | `Next.js (React)` | 业界标杆，路由管理方便，Vercel 部署无缝衔接，性能极佳。 |
| **样式** | `Tailwind CSS` | 完美支持快速迭代和 MVP 搭建，类名编写速度快，易于实现暗黑模式。 |
| **动画** | `Framer Motion` | 业界最强大的 React 动画库，用于实现平滑的组件出场动画、Hover 效果以及 Prompt 窗口的丝滑展开。 |
| **特效** | `Lucide React` | 极简、清爽的图标库，用于各种小 icon。 |
| **部署** | `Vercel` | Next.js 官方托管平台，链接 GitHub 即实现自动 CI/CD 部署，生成公开链接。 |

---

## 三、开发里程碑 (Milestones)

### Phase 1: 基础框架与首屏搭建 (MVP v0.1)
- [ ] 初始化 Next.js 项目并配置 Tailwind CSS。
- [ ] 搭建全局导航栏（Logo + 5 个锚点跳转链接）与 Footer。
- [ ] 完成 **Hero 区**（首屏）：姓名、头像、打字机动效一句话定位、CTA 按钮。
- [ ] 部署到 Vercel 获取初始 Public Link。

### Phase 2: 核心内容模块注入 (MVP v0.2)
- [ ] **About 区**：个人简介与核心能力卡片。
- [ ] **Experience 区**：实习经历时间线/卡片。
- [ ] **Projects 区**：项目卡片，提取核心亮点与数据。
- [ ] **Skills / Contact 区**：技能矩阵展示与联系方式。

### Phase 3: 注入灵魂 — "AI Dev Mode" 与交互打磨 (V1.0)
- [ ] 实现右上角 **AI Dev Mode 切换开关**状态管理。
- [ ] 实现 Prompt 展示视窗：当开启时，配合 `IntersectionObserver` 监听当前滚动位置，动态切换并以代码高亮形式展示对应的生成 Prompt。
- [ ] 加入整页进入动画、滚动渐现 (Scroll Reveal) 和卡片 Hover 动效。
- [ ] 移动端响应式极致适配。

### Phase 4: Internationalization (i18n) (Active)
为了适配出海及香港求职需求，加入中英文无缝切换能力：
- [ ] 构建全局 `LanguageContext` 状态管理 (zh / en)。
- [ ] 分离中文与英文硬编码文案，形成 `locales/zh.ts` 和 `locales/en.ts` 字典文件。
- [ ] 在 [Header](file:///Users/qianjincheng/Library/Mobile%20Documents/com~apple~CloudDocs/%E5%AE%9E%E4%B9%A0/portfolio-web/src/components/Header.tsx#19-168) 组件中新增语言切换按钮 (Language Switcher)。
- [ ] 将页面所有业务组件 (Hero, About, TechStack, Experience, Projects, Contact, RightNav) 接入 `useLanguage` Hook 实现文案的动态读取与切换。

---

## 四、Verification Plan (如何验证)
由于这是前端 UI 项目，验证主要方式为**本地运行截图与最终部署链接**。
1. **自动化/构建测试**：运行 `npm run build` 确保没有 TypeScript/Lint 错误，静态编译成功。
2. **人工 UI 验证**：启动本地服务器 `npm run dev`。
   - 验证响应式：使用 Chrome 开发者工具切换不同设备尺寸，确保不出现横向滚动条。
   - 验证功能：点击每一个导航链接确保锚点跳转正常；切换 AI Dev Mode 开关，确保 Prompt 内容准确切换。
3. **线上验证**：通过 Vercel 提供的 Public URL（可在手机端打开测试）。

---

## 请确认：
上面的思路可以吗？尤其是 **"AI Dev Mode 切换" + "暗夜科技风"** 的组合。
如果没有问题，我这就开始用 Next.js 帮你把代码写出来！
